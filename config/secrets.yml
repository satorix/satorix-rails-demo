# Use environment variables for 'real' secrets, and placeholders in this file.

# Shared secrets are available across all environments.
shared:

  # Used for verifying the integrity of signed cookies.
  # If you change this key, all old signed cookies will become invalid!
  # Make sure the secret is at least 30 characters and all random,
  # no regular words or you'll be exposed to dictionary attacks.
  # You can use `rails secret` to generate a secure secret key.
  secret_key_base: <%= ENV['SECRET_KEY_BASE'] || 'a13c17bc2bd6c1f751b3d10c0cc6b8103a064540724a58a4bb824f4ad210d9c006a43c57c245d6e2b5954193f7fc7e8d74353d97834d71db8529f7091dce2650' %>

